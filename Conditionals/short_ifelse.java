public class Shortifelse{
	public static void main(String[] args){
		int time = 10;
		/*Podemos dar el resultado de evaluar una expresion a una variable si tenemos un condicional de una sola linea
		Se escribe de la siguiente manera
		variable = (condition) ? Expresion_si_V : Expresion_si_F;
		Ejemplo:
		*/
		String result = (time < 18) ? "Buenas tardes" : "Buenos dias";
		System.out.println(result);
	}
}

